doctype html
html(lang=meta.locale)
   head
      include head/settings
      include head/seo
      include head/favicon
      include head/resources

   body

      - var hasHeader = meta.namespace !== 'homepage';

      main(id=meta.namespace class=hasHeader&&'pt6 pt5-ns pt6-l')

         if hasHeader
            include components/header

         block content